<h1>Products<h1>

    <%= link_to "New product", new_product_path %>

<div id="Products">
  <% @products.each do |product| %>
    <div>
        <%= link_to product.name, product %>
      </a>
    </div>
  <%end%>
</div>

